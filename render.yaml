services:
  - type: web
    name: crypto-tax-backend
    runtime: python
    rootDir: crypto-tax-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
